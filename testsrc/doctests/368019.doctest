// Regression test for bug https://bugzilla.mozilla.org/show_bug.cgi?id=368019
//  - regular expression (regex) parses /[/]/ incorrectly
js> re = (/[^/]*/) instanceof RegExp;
true
js> re = (/[/]/) instanceof RegExp;
true
js> re = (/\[/) instanceof RegExp;
true
